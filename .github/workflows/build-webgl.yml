name: Unity 6 WebGL Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build Unity WebGL
        uses: game-ci/unity-builder@v4
        env:
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}        # si licence personnelle sur runner cloud
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}  # idem
        with:
          version: 6000.2.3f1
          targetPlatform: WebGL
          buildName: Web-poki
          buildsPath: Builds       # <-- remplace 'buildPath' par 'buildsPath'
          customParameters: -executeMethod BuildWebGL.Build
          allowDirtyBuild: true

      - name: Upload WebGL build
        uses: actions/upload-artifact@v4
        with:
          name: WebGL-Build
          path: Builds/Web-poki
